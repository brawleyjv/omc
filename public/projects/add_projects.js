// Ensure no popup or prompt for customer's name exists
// Example of code to remove:
document.getElementById('addProjectForm').addEventListener('submit', function (e) {
    // Removed any logic prompting for customer's name
    // ...existing code...
});